# a3.2 config-feature verification matrix

Run 2026-08-18 on the Pixel_7 emulator (API 34) against the extension repo's
mock backend (`scripts/mock-backend.sh`, used verbatim), debug build.
Evidence channels: the run-as prefs oracle
(`adb shell run-as one.moveo.studywrapper cat shared_prefs/studywrapper.xml`),
the debug logcat feeds (`moveo-backend` for bridge/HTTP/lead lines,
`moveo-events` for the M5 event spy), screenshots, and the studycore JVM
tests where the wrapper's whole job is payload pass-through.

| Feature | Study / method | Result |
|---|---|---|
| url_match target | TESTCODE1234, navigate `/checkout/x/thanks` | ✅ M4/M5: `targetFired`, `leadOutDueAt`=+2s persisted before the delay, lead-out launched |
| event_match target (interaction) | EVENTMATCH01 `{eA: click, eT: h1}`, tap the h1 | ✅ spy shows `click h1`, `bridge: target`, lead-out +2s, `leadOutDueAt` cleared |
| event_match target (synthetic page_view) | EVENTPAGE001 `{eA: page_view, sc: /checkout*}`, navigate | ✅ target fired from the bootstrap's early-event compensation, before the new page's init ping |
| Lead-in | every enrollment above | ✅ Custom Tab on first tracked-origin visit; `leadInShownAt` at launch; re-arms per enrollment |
| Lead-out | url_match + both event_match runs | ✅ 2 s after target; `leadOutShownAt` at launch, due-at cleared, never repeats |
| appendParticipantId | TESTCODE1234 (true in mock config) | ✅ `lead: LEAD_IN https://example.org/intro?participantId=p_…` (debug lead log; OS redacts intent URIs, hence the log line) |
| Own-tag yield | live `javascript:window.MoveoOne.init()` in the study page (simulates a site's own tag) | ✅ `bridge: ownTag example.com`, host persisted in `ownTagHosts`, next load has **no** tag init. Survives replacement, cleared on leave — same lifecycle as iOS |
| Semantic-group rules | mock's `{container: div, name: [{source: h1}]}` | ✅ spy events carry `sg=example_domain` instead of the heuristic sg |
| additionalTrackedElements | mock's `{selector: "body > div", type: page_wrapper}` | ✅ spy shows `appear eT=page_wrapper` with the full page text as eV |
| excludeDetailedTracking false | valid-full fixture in payload | ✅ `ScriptBuilderTests.payloadCarriesBridgeMapping` |
| excludeDetailedTracking true / absent | JVM pin tests (mock can't serve them) | ✅ `storageSourceAndExcludeVariantsPassThrough` + `absentExcludeDetailedTrackingStaysAbsent` — absent stays absent so the tag applies its per-type default; behavior itself is tag-internal (normative in the extension repo) |
| storageSource local | live runs | ✅ fresh `start_session` per browser session observed in the M5 capture |
| storageSource session | JVM pin test | ✅ verbatim pass-through into the payload; tag-internal behavior |
| prepositionPath | iOS-first field (wrapper apps only — not in the extension schema); ported in M1 | ✅ `StudyConfig.startUrl` + validator identical to iOS (root default, missing leading `/` tolerated, empty/null collapse, wrong type rejected) — pinned by the ported `ConfigValidatorTests` prepositionPath cases; `browserStartUrl` uses it on every browser open (mock configs leave it absent → root, so the live runs exercise the default path; the verbatim mock cannot serve a configured path — same live-coverage limit as iOS) |
| Replacement flow | enrolled 4831 → activate EVENTMATCH01 | ✅ new enroll 201, `activeStudy` swapped to st_eventmatch, lead-in re-armed, `targetFired` reset (warning copy verified in M3) |
| Short-code acceptance | 4831 | ✅ same study served, enroll 201, browser + tag init |
| Ended (config status) | ENDED0CODE01 | ✅ "This study has finished / …no longer running. Thank you for your interest." |
| Kill switch ended | `/__end` + foreground | ✅ verified in M4 (endedStudy written, activeStudy cleared) |
| Revoked (config 404) | point base at a 404ing path, background→foreground | ✅ `endedStudy.revoked=true` with lead history carried over; "Study no longer available / …If you think this is a mistake, contact study support." |
| Enrollment closed | CLOSED0CODE1 (enroll 403) | ✅ "Study is full / This study is no longer accepting participants." |
| Invalid config | BROKEN0CODE1 (missing token) | ✅ "Study can't be loaded / This study's configuration isn't valid. Contact study support." |
| needsAppUpdate | FUTURE0CODE1 (schemaVersion 99) | ✅ "App update needed / This study requires a newer version of this app. Please update it and try again." |

Open (not blocking a3.2): M5 desktop-HAR diff (user-side capture);
a3.3 data-team cohort-separability confirmation; a0.2 backend sign-off for
`client: "android"` enroll admission.
